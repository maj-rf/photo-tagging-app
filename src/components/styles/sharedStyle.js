import styled from 'styled-components';

const SharedSection = styled.section`
  border: 2px solid #fff;
  border-radius: 1em 1em;
  width: clamp(350px, 60vw, 100%);
  font-size: clamp(14px, 2vw, 1.2rem);
  margin-bottom: 1em;
`;

export { SharedSection };
